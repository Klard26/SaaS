openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: Klard – Booking platform for consultants
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: categories
    description: Service categories
  - name: providers
    description: Provider operations
  - name: services
    description: Service offerings
  - name: availability
    description: Time slot operations
  - name: bookings
    description: Booking operations
  - name: reviews
    description: Review operations
  - name: ai
    description: AI offer generation
  - name: dashboard
    description: Dashboard summaries
  - name: billing
    description: Subscription and payment operations
  - name: calendar
    description: Calendar synchronization (iCalendar feeds)
  - name: storage
    description: Object storage uploads
  - name: assessments
    description: Saved building energy assessments
  - name: invoices
    description: Provider invoices (§ 14 UStG)
  - name: admin
    description: Platform operator admin dashboard
  - name: energie
    description: WattWechsel energy-switching for property managers (Hausverwalter/Bestandshalter)

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /categories:
    get:
      operationId: listCategories
      tags: [categories]
      summary: List all categories
      responses:
        "200":
          description: List of categories
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Category"

  /service-templates:
    get:
      operationId: listServiceTemplates
      tags: [services]
      summary: List service templates for a given category slug
      parameters:
        - name: category
          in: query
          required: true
          schema: { type: string }
      responses:
        "200":
          description: List of templates
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ServiceTemplate"

  /storage/uploads/request-url:
    post:
      operationId: requestUploadUrl
      tags: [storage]
      summary: Request a presigned upload URL for object storage
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UploadUrlRequest"
      responses:
        "200":
          description: Upload URL
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadUrlResponse"

  /providers:
    get:
      operationId: listProviders
      tags: [providers]
      summary: Search/list providers
      parameters:
        - name: zip
          in: query
          schema: { type: string }
        - name: city
          in: query
          schema: { type: string }
        - name: category
          in: query
          schema: { type: string }
        - name: q
          in: query
          schema: { type: string }
        - name: minPrice
          in: query
          schema: { type: number }
        - name: maxPrice
          in: query
          schema: { type: number }
        - name: limit
          in: query
          schema: { type: integer }
        - name: offset
          in: query
          schema: { type: integer }
      responses:
        "200":
          description: List of providers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProviderSummary"
    post:
      operationId: createProvider
      tags: [providers]
      summary: Create a provider profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProviderInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Provider"

  /providers/{id}:
    get:
      operationId: getProvider
      tags: [providers]
      summary: Get provider details
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Provider detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Provider"
        "404":
          description: Not found
    patch:
      operationId: updateProvider
      tags: [providers]
      summary: Update provider profile
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProviderUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Provider"

  /providers/me:
    get:
      operationId: getMyProviderProfile
      tags: [providers]
      summary: Get the authenticated user's provider profile
      responses:
        "200":
          description: Provider profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Provider"
        "404":
          description: Not found (not yet a provider)

  /providers/{id}/services:
    get:
      operationId: listProviderServices
      tags: [services]
      summary: List services for a provider
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: List of services
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Service"
    post:
      operationId: createService
      tags: [services]
      summary: Add a service to a provider
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ServiceInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Service"

  /services/{id}:
    patch:
      operationId: updateService
      tags: [services]
      summary: Update a service
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ServiceUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Service"
    delete:
      operationId: deleteService
      tags: [services]
      summary: Delete a service
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "204":
          description: Deleted

  /providers/{id}/availability:
    get:
      operationId: listAvailability
      tags: [availability]
      summary: Get available time slots for a provider
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: List of time slots
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TimeSlot"

  /availability:
    post:
      operationId: createTimeSlot
      tags: [availability]
      summary: Create a time slot
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TimeSlotInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TimeSlot"
    delete:
      operationId: deleteTimeSlot
      tags: [availability]
      summary: Delete a time slot
      parameters:
        - name: slotId
          in: query
          required: true
          schema: { type: integer }
      responses:
        "204":
          description: Deleted

  /bookings:
    post:
      operationId: createBooking
      tags: [bookings]
      summary: Create a booking
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BookingInput"
      responses:
        "201":
          description: Booking created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Booking"
    get:
      operationId: listMyBookings
      tags: [bookings]
      summary: List bookings for the authenticated user (as customer)
      responses:
        "200":
          description: List of bookings
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Booking"

  /bookings/{id}:
    get:
      operationId: getBooking
      tags: [bookings]
      summary: Get booking details
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Booking
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Booking"

  /bookings/{id}/status:
    patch:
      operationId: updateBookingStatus
      tags: [bookings]
      summary: Update booking status (confirm / cancel)
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BookingStatusUpdate"
      responses:
        "200":
          description: Updated booking
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Booking"

  /providers/{id}/bookings:
    get:
      operationId: listProviderBookings
      tags: [bookings]
      summary: List bookings for a provider (dashboard)
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: List of bookings
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Booking"

  /providers/{id}/reviews:
    get:
      operationId: listProviderReviews
      tags: [reviews]
      summary: List reviews for a provider
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Reviews
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Review"

  /reviews:
    post:
      operationId: createReview
      tags: [reviews]
      summary: Leave a review for a completed booking
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReviewInput"
      responses:
        "201":
          description: Review created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Review"

  /ai/offer:
    post:
      operationId: generateAiOffer
      tags: [ai]
      summary: Generate an AI offer for a service inquiry
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AiOfferRequest"
      responses:
        "200":
          description: Generated offer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiOfferResponse"

  /dashboard/provider/{id}:
    get:
      operationId: getProviderDashboard
      tags: [dashboard]
      summary: Get provider dashboard summary
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Dashboard data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProviderDashboard"

  /providers/me/subscription/checkout:
    post:
      operationId: createSubscriptionCheckout
      tags: [billing]
      summary: Create a Stripe Checkout Session for the Premium provider subscription
      responses:
        "200":
          description: Checkout session created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckoutSession"

  /providers/me/subscription:
    get:
      operationId: getMySubscription
      tags: [billing]
      summary: Get current provider subscription status
      responses:
        "200":
          description: Subscription status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionStatus"
    delete:
      operationId: cancelMySubscription
      tags: [billing]
      summary: Cancel premium subscription at period end
      responses:
        "200":
          description: Cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionStatus"

  /bookings/{id}/payment/checkout:
    post:
      operationId: createBookingCheckout
      tags: [billing]
      summary: Create a Stripe Checkout Session for a booking payment
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Checkout session created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckoutSession"

  /providers/{id}/calendar.ics:
    get:
      operationId: getProviderCalendarFeed
      tags: [calendar]
      summary: iCal feed of confirmed bookings for a provider (token via ?token=)
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: iCalendar (.ics) feed
          content:
            text/calendar:
              schema: { type: string }

  /bookings/{id}/calendar.ics:
    get:
      operationId: getBookingCalendarFile
      tags: [calendar]
      summary: Download .ics file for a single booking
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: iCalendar (.ics) file
          content:
            text/calendar:
              schema: { type: string }

  /assessments:
    get:
      operationId: listAssessments
      tags: [assessments]
      summary: List the current user's saved building assessments
      responses:
        "200":
          description: List of assessments
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Assessment"
    post:
      operationId: createAssessment
      tags: [assessments]
      summary: Save a new building assessment for the current user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssessmentInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Assessment"

  /assessments/{id}:
    get:
      operationId: getAssessment
      tags: [assessments]
      summary: Get a single saved assessment (owner only)
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Assessment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Assessment"
    delete:
      operationId: deleteAssessment
      tags: [assessments]
      summary: Delete a saved assessment (owner only)
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "204":
          description: Deleted

  /gebaeudecheck/credits:
    get:
      operationId: getGebaeudecheckCredits
      tags: [gebaeudecheck]
      summary: Current user's Gebäudecheck report credit balance + package catalog
      responses:
        "200":
          description: Credit balance and available packages
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GebaeudecheckCredits"

  /gebaeudecheck/checkout:
    post:
      operationId: createGebaeudecheckCheckout
      tags: [gebaeudecheck]
      summary: Create a Stripe Checkout Session to buy a Gebäudecheck credit package
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GebaeudecheckCheckoutInput"
      responses:
        "200":
          description: Checkout session created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckoutSession"

  /gebaeudecheck/reconcile:
    post:
      operationId: reconcileGebaeudecheckOrder
      tags: [gebaeudecheck]
      summary: Grant credits after a successful Checkout redirect (idempotent)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GebaeudecheckReconcileInput"
      responses:
        "200":
          description: Reconcile result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GebaeudecheckReconcileResult"

  /offers/accept:
    post:
      operationId: acceptOffer
      tags: [offers]
      summary: Customer legally accepts an offer (selected services) from a provider
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AcceptOfferInput"
      responses:
        "201":
          description: Offer accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OfferAcceptance"

  /offers/mine:
    get:
      operationId: listMyOffers
      tags: [offers]
      summary: List offers the current user has accepted
      responses:
        "200":
          description: List of accepted offers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OfferAcceptance"

  /immobilien-kunde/me:
    get:
      operationId: getMyImmobilienKunde
      tags: [immobilienKunde]
      summary: Current user's Immobilien-Kundenprofil (Hausverwalter/Bestandshalter)
      responses:
        "200":
          description: The profile, or null if none exists
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/ImmobilienKunde"
                  - type: "null"
    put:
      operationId: upsertMyImmobilienKunde
      tags: [immobilienKunde]
      summary: Create or update the current user's Immobilien-Kundenprofil
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ImmobilienKundeInput"
      responses:
        "200":
          description: The saved profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImmobilienKunde"

  /providers/me/invoice-settings:
    get:
      operationId: getMyInvoiceSettings
      tags: [invoices]
      summary: Get the authenticated provider's invoice settings
      responses:
        "200":
          description: Invoice settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvoiceSettings"
    patch:
      operationId: updateMyInvoiceSettings
      tags: [invoices]
      summary: Update the authenticated provider's invoice settings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InvoiceSettingsUpdate"
      responses:
        "200":
          description: Updated invoice settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvoiceSettings"

  /invoices/me:
    get:
      operationId: listMyProviderInvoices
      tags: [invoices]
      summary: List invoices issued by the authenticated provider
      responses:
        "200":
          description: List of invoices
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Invoice"

  /invoices/customer:
    get:
      operationId: listMyCustomerInvoices
      tags: [invoices]
      summary: List invoices received by the authenticated customer
      responses:
        "200":
          description: List of invoices
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Invoice"

  /invoices/{id}/pdf:
    get:
      operationId: getInvoicePdf
      tags: [invoices]
      summary: Download an invoice PDF (provider owner or booking customer)
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: PDF
          content:
            application/pdf:
              schema:
                type: string
                format: binary

  /bookings/{id}/invoice:
    post:
      operationId: issueInvoiceForBooking
      tags: [invoices]
      summary: Manually issue (or re-fetch) the invoice for a paid booking
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Invoice
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Invoice"

  /dashboard/stats:
    get:
      operationId: getPlatformStats
      tags: [dashboard]
      summary: Platform-level stats (total providers, categories, bookings)
      responses:
        "200":
          description: Platform stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformStats"

  /admin/me:
    get:
      operationId: getAdminMe
      tags: [admin]
      summary: Returns whether the current user is an admin
      responses:
        "200":
          description: Admin status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminMe"

  /admin/stats:
    get:
      operationId: getAdminStats
      tags: [admin]
      summary: Platform-wide aggregate statistics
      responses:
        "200":
          description: Stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminStats"

  /admin/timeseries:
    get:
      operationId: getAdminTimeseries
      tags: [admin]
      summary: Daily bookings and revenue time-series
      parameters:
        - name: days
          in: query
          schema: { type: integer, minimum: 1, maximum: 365, default: 30 }
      responses:
        "200":
          description: Daily points
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AdminTimeseriesPoint"

  /admin/providers:
    get:
      operationId: listAdminProviders
      tags: [admin]
      summary: All providers with usage metrics
      responses:
        "200":
          description: Providers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AdminProviderRow"

  /admin/customers:
    get:
      operationId: listAdminCustomers
      tags: [admin]
      summary: Distinct customers with booking aggregates
      responses:
        "200":
          description: Customers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AdminCustomerRow"

  /admin/bookings:
    get:
      operationId: listAdminBookings
      tags: [admin]
      summary: Recent bookings across the platform
      parameters:
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 500, default: 100 }
        - name: status
          in: query
          schema: { type: string, enum: [pending, confirmed, completed, cancelled] }
      responses:
        "200":
          description: Bookings
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AdminBookingRow"

  /admin/categories:
    get:
      operationId: listAdminCategories
      tags: [admin]
      summary: Category breakdown with booking and revenue counts
      responses:
        "200":
          description: Categories
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AdminCategoryRow"

  /energie/verwalter/me:
    get:
      operationId: getMyVerwalter
      tags: [energie]
      summary: Get the current user's Verwalter (energy portfolio) account, or null
      responses:
        "200":
          description: Verwalter account or null
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/Verwalter"
                  - type: "null"
    post:
      operationId: createMyVerwalter
      tags: [energie]
      summary: Onboard the current user as a Verwalter
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VerwalterInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Verwalter"
    patch:
      operationId: updateMyVerwalter
      tags: [energie]
      summary: Update the current user's Verwalter account
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VerwalterInput"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Verwalter"

  /energie/portfolio:
    get:
      operationId: getEnergiePortfolio
      tags: [energie]
      summary: Portfolio KPIs and full object/meter/contract tree
      responses:
        "200":
          description: Portfolio overview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PortfolioOverview"

  /energie/objekte:
    post:
      operationId: createObjekt
      tags: [energie]
      summary: Add a building (Objekt) to the portfolio
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ObjektInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Objekt"

  /energie/objekte/{id}:
    delete:
      operationId: deleteObjekt
      tags: [energie]
      summary: Delete a building (owner only)
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "204":
          description: Deleted

  /energie/zaehlpunkte:
    post:
      operationId: createZaehlpunkt
      tags: [energie]
      summary: Add a metering point (Zählpunkt) to a building
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ZaehlpunktInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Zaehlpunkt"

  /energie/zaehlpunkte/{id}:
    delete:
      operationId: deleteZaehlpunkt
      tags: [energie]
      summary: Delete a metering point (owner only)
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "204":
          description: Deleted

  /energie/vertraege:
    post:
      operationId: createVertrag
      tags: [energie]
      summary: Record the current supply contract for a metering point
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VertragInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vertrag"

  /energie/vollmachten:
    get:
      operationId: listVollmachten
      tags: [energie]
      summary: List the Verwalter's powers of attorney
      responses:
        "200":
          description: List of Vollmachten
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Vollmacht"
    post:
      operationId: createVollmacht
      tags: [energie]
      summary: Grant a new power of attorney
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VollmachtInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vollmacht"

  /energie/vollmachten/{id}/status:
    patch:
      operationId: updateVollmachtStatus
      tags: [energie]
      summary: Change a power of attorney's status (activate, pause, revoke)
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VollmachtStatusUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vollmacht"

  /energie/wechsel:
    get:
      operationId: listWechselvorgaenge
      tags: [energie]
      summary: List switch processes across the portfolio
      responses:
        "200":
          description: List of switch processes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Wechselvorgang"

  /energie/wechsel/analyse:
    post:
      operationId: starteAnalyse
      tags: [energie]
      summary: Start an AI tariff analysis for a metering point
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AnalyseInput"
      responses:
        "201":
          description: Switch process created with recommendation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Wechselvorgang"

  /energie/wechsel/{id}/freigeben:
    post:
      operationId: freigebenWechsel
      tags: [energie]
      summary: Approve a switch (runs the simulated market execution)
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Updated switch process
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Wechselvorgang"

  /energie/wechsel/{id}/ablehnen:
    post:
      operationId: ablehnenWechsel
      tags: [energie]
      summary: Reject a recommended switch
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Updated switch process
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Wechselvorgang"

  /energie/wechsel/{id}/widersprechen:
    post:
      operationId: widersprechenWechsel
      tags: [energie]
      summary: Object to a switch within the objection window
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Updated switch process
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Wechselvorgang"

  /energie/audit:
    get:
      operationId: listAuditLog
      tags: [energie]
      summary: List the Verwalter's audit trail
      responses:
        "200":
          description: Audit entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AuditEntry"

  /energie/tarife:
    get:
      operationId: listTarife
      tags: [energie]
      summary: List available tariff offers (transparency feed)
      parameters:
        - name: sparte
          in: query
          required: false
          schema: { type: string }
      responses:
        "200":
          description: Tariff offers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TarifAngebot"

components:
  schemas:
    AdminMe:
      type: object
      required: [isAdmin]
      properties:
        isAdmin: { type: boolean }

    Verwalter:
      type: object
      required: [id, clerkUserId, firma, typ, erlaubnis34c, provisionsmodell, aktiv, createdAt]
      properties:
        id: { type: integer }
        clerkUserId: { type: string }
        firma: { type: string }
        typ: { type: string }
        handelsregisterNr: { type: ["string", "null"] }
        ustId: { type: ["string", "null"] }
        erlaubnis34c: { type: boolean }
        strasse: { type: ["string", "null"] }
        plz: { type: ["string", "null"] }
        ort: { type: ["string", "null"] }
        email: { type: ["string", "null"] }
        telefon: { type: ["string", "null"] }
        provisionsmodell: { type: string }
        aktiv: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    VerwalterInput:
      type: object
      required: [firma, typ]
      properties:
        firma: { type: string, minLength: 1, maxLength: 200 }
        typ: { type: string, enum: [hausverwaltung, weg_verwalter, bestandshalter, gewerbe] }
        handelsregisterNr: { type: ["string", "null"] }
        ustId: { type: ["string", "null"] }
        erlaubnis34c: { type: boolean }
        strasse: { type: ["string", "null"] }
        plz: { type: ["string", "null"] }
        ort: { type: ["string", "null"] }
        email: { type: ["string", "null"] }
        telefon: { type: ["string", "null"] }
        provisionsmodell: { type: string, enum: [saas_flat, erfolgsprovision, hybrid] }

    Objekt:
      type: object
      required: [id, verwalterId, bezeichnung, strasse, plz, ort, wegBeschluss, createdAt]
      properties:
        id: { type: integer }
        verwalterId: { type: integer }
        bezeichnung: { type: string }
        strasse: { type: string }
        plz: { type: string }
        ort: { type: string }
        wegBeschluss: { type: boolean }
        wegBeschlussDatum: { type: ["string", "null"] }
        notiz: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }

    ObjektInput:
      type: object
      required: [bezeichnung, strasse, plz, ort]
      properties:
        bezeichnung: { type: string, minLength: 1, maxLength: 200 }
        strasse: { type: string, minLength: 1 }
        plz: { type: string, minLength: 4, maxLength: 5 }
        ort: { type: string, minLength: 1 }
        wegBeschluss: { type: boolean }
        wegBeschlussDatum: { type: ["string", "null"] }
        notiz: { type: ["string", "null"] }

    Zaehlpunkt:
      type: object
      required: [id, objektId, sparte, art, createdAt]
      properties:
        id: { type: integer }
        objektId: { type: integer }
        sparte: { type: string }
        art: { type: string }
        maloId: { type: ["string", "null"] }
        zaehlernummer: { type: ["string", "null"] }
        jahresverbrauchKwh: { type: ["number", "null"] }
        jahresverbrauchLiter: { type: ["number", "null"] }
        netzbetreiber: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }

    ZaehlpunktInput:
      type: object
      required: [objektId, sparte]
      properties:
        objektId: { type: integer }
        sparte: { type: string, enum: [strom, gas, heizoel, fernwaerme] }
        art: { type: string, enum: [allgemeinstrom, mieterstrom, gewerbe, heizung, waermepumpe, sonstige] }
        maloId: { type: ["string", "null"] }
        zaehlernummer: { type: ["string", "null"] }
        jahresverbrauchKwh: { type: ["number", "null"] }
        jahresverbrauchLiter: { type: ["number", "null"] }
        netzbetreiber: { type: ["string", "null"] }

    Vertrag:
      type: object
      required: [id, zaehlpunktId, versorger, kuendigungsfristTage, istAktiv, quelle, createdAt]
      properties:
        id: { type: integer }
        zaehlpunktId: { type: integer }
        versorger: { type: string }
        tarifname: { type: ["string", "null"] }
        arbeitspreisCtKwh: { type: ["number", "null"] }
        grundpreisEurJahr: { type: ["number", "null"] }
        vertragsbeginn: { type: ["string", "null"] }
        erstlaufzeitEnde: { type: ["string", "null"] }
        kuendigungsfristTage: { type: integer }
        naechsterKuendigungstermin: { type: ["string", "null"] }
        preisgarantieBis: { type: ["string", "null"] }
        istAktiv: { type: boolean }
        quelle: { type: string }
        createdAt: { type: string, format: date-time }

    VertragInput:
      type: object
      required: [zaehlpunktId, versorger]
      properties:
        zaehlpunktId: { type: integer }
        versorger: { type: string, minLength: 1 }
        tarifname: { type: ["string", "null"] }
        arbeitspreisCtKwh: { type: ["number", "null"] }
        grundpreisEurJahr: { type: ["number", "null"] }
        vertragsbeginn: { type: ["string", "null"] }
        erstlaufzeitEnde: { type: ["string", "null"] }
        kuendigungsfristTage: { type: ["integer", "null"] }
        naechsterKuendigungstermin: { type: ["string", "null"] }
        preisgarantieBis: { type: ["string", "null"] }

    Vollmacht:
      type: object
      required: [id, verwalterId, status, modus, darfKuendigen, darfAbschliessen, darfSonderkuendigung, darfDatenAbfragen, darfBankdatenWeitergeben, widerspruchsfristTage, createdAt]
      properties:
        id: { type: integer }
        verwalterId: { type: integer }
        objektId: { type: ["integer", "null"] }
        sparte: { type: ["string", "null"] }
        status: { type: string }
        modus: { type: string }
        darfKuendigen: { type: boolean }
        darfAbschliessen: { type: boolean }
        darfSonderkuendigung: { type: boolean }
        darfDatenAbfragen: { type: boolean }
        darfBankdatenWeitergeben: { type: boolean }
        widerspruchsfristTage: { type: integer }
        gueltigAb: { type: ["string", "null"] }
        gueltigBis: { type: ["string", "null"] }
        erteiltAm: { type: ["string", "null"], format: date-time }
        widerrufenAm: { type: ["string", "null"], format: date-time }
        widerrufGrund: { type: ["string", "null"] }
        dokumentPfad: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }

    VollmachtInput:
      type: object
      required: [modus]
      properties:
        objektId: { type: ["integer", "null"] }
        sparte: { type: ["string", "null"], enum: [strom, gas, heizoel, fernwaerme, null] }
        modus: { type: string, enum: [nur_vorschlag, freigabe_erforderlich, vollautomatisch] }
        darfKuendigen: { type: boolean }
        darfAbschliessen: { type: boolean }
        darfSonderkuendigung: { type: boolean }
        darfDatenAbfragen: { type: boolean }
        darfBankdatenWeitergeben: { type: boolean }
        widerspruchsfristTage: { type: integer, minimum: 1, maximum: 60 }
        gueltigAb: { type: ["string", "null"] }
        gueltigBis: { type: ["string", "null"] }

    VollmachtStatusUpdate:
      type: object
      required: [status]
      properties:
        status: { type: string, enum: [aktiv, pausiert, widerrufen, abgelaufen] }
        widerrufGrund: { type: ["string", "null"] }

    Wechselvorgang:
      type: object
      required: [id, zaehlpunktId, vollmachtId, status, createdAt]
      properties:
        id: { type: integer }
        zaehlpunktId: { type: integer }
        vollmachtId: { type: integer }
        altVertragId: { type: ["integer", "null"] }
        status: { type: string }
        empfVersorger: { type: ["string", "null"] }
        empfTarif: { type: ["string", "null"] }
        empfArbeitspreisCtKwh: { type: ["number", "null"] }
        empfGrundpreisEurJahr: { type: ["number", "null"] }
        ersparnisEurJahr: { type: ["number", "null"] }
        ersparnisProzent: { type: ["number", "null"] }
        anzahlVerglicheneAnbieter: { type: ["integer", "null"] }
        kiBegruendung: { type: ["string", "null"] }
        freigegebenAm: { type: ["string", "null"], format: date-time }
        widerspruchBis: { type: ["string", "null"], format: date-time }
        neuVertragId: { type: ["integer", "null"] }
        abgeschlossenAm: { type: ["string", "null"], format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    AnalyseInput:
      type: object
      required: [zaehlpunktId]
      properties:
        zaehlpunktId: { type: integer }
        vollmachtId: { type: ["integer", "null"] }

    AuditEntry:
      type: object
      required: [id, akteur, aktion, zeitpunkt]
      properties:
        id: { type: integer }
        verwalterId: { type: ["integer", "null"] }
        vollmachtId: { type: ["integer", "null"] }
        wechselId: { type: ["integer", "null"] }
        akteur: { type: string }
        aktion: { type: string }
        details:
          type: ["object", "null"]
          additionalProperties: true
        zeitpunkt: { type: string, format: date-time }

    TarifAngebot:
      type: object
      required: [id, sparte, versorger, tarifname, arbeitspreisCtKwh, grundpreisEurJahr, oekostrom]
      properties:
        id: { type: integer }
        sparte: { type: string }
        versorger: { type: string }
        tarifname: { type: string }
        arbeitspreisCtKwh: { type: number }
        grundpreisEurJahr: { type: number }
        laufzeitMonate: { type: ["integer", "null"] }
        preisgarantieMonate: { type: ["integer", "null"] }
        oekostrom: { type: boolean }
        minVerbrauchKwh: { type: ["number", "null"] }
        maxVerbrauchKwh: { type: ["number", "null"] }
        plzGebiet: { type: ["string", "null"] }

    PortfolioKpi:
      type: object
      required: [anzahlObjekte, anzahlZaehlpunkte, aktiveVertraege, kuendigungen60Tage, realisierteErsparnisEur, potenzialErsparnisEur]
      properties:
        anzahlObjekte: { type: integer }
        anzahlZaehlpunkte: { type: integer }
        aktiveVertraege: { type: integer }
        kuendigungen60Tage: { type: integer }
        realisierteErsparnisEur: { type: number }
        potenzialErsparnisEur: { type: number }

    PortfolioZaehlpunkt:
      type: object
      required: [zaehlpunkt]
      properties:
        zaehlpunkt: { $ref: "#/components/schemas/Zaehlpunkt" }
        vertrag:
          oneOf:
            - $ref: "#/components/schemas/Vertrag"
            - type: "null"

    PortfolioObjekt:
      type: object
      required: [objekt, zaehlpunkte]
      properties:
        objekt: { $ref: "#/components/schemas/Objekt" }
        zaehlpunkte:
          type: array
          items:
            $ref: "#/components/schemas/PortfolioZaehlpunkt"

    PortfolioOverview:
      type: object
      required: [kpi, objekte]
      properties:
        kpi: { $ref: "#/components/schemas/PortfolioKpi" }
        objekte:
          type: array
          items:
            $ref: "#/components/schemas/PortfolioObjekt"

    AdminStats:
      type: object
      required: [bookings, providers, customers, categories, reviews, invoices]
      properties:
        bookings:
          type: object
          required: [total, pending, confirmed, completed, cancelled, revenueAll, revenuePaidCents]
          properties:
            total: { type: integer }
            pending: { type: integer }
            confirmed: { type: integer }
            completed: { type: integer }
            cancelled: { type: integer }
            revenueAll: { type: integer }
            revenuePaidCents: { type: integer }
        providers:
          type: object
          required: [total, premium, verified]
          properties:
            total: { type: integer }
            premium: { type: integer }
            verified: { type: integer }
        customers:
          type: object
          required: [total]
          properties:
            total: { type: integer }
        categories:
          type: object
          required: [total]
          properties:
            total: { type: integer }
        reviews:
          type: object
          required: [total, averageRating]
          properties:
            total: { type: integer }
            averageRating: { type: number }
        invoices:
          type: object
          required: [total, storno, totalCents]
          properties:
            total: { type: integer }
            storno: { type: integer }
            totalCents: { type: integer }

    AdminTimeseriesPoint:
      type: object
      required: [day, bookings, paidRevenueCents]
      properties:
        day: { type: string }
        bookings: { type: integer }
        paidRevenueCents: { type: integer }

    AdminProviderRow:
      type: object
      required: [id, displayName, email, category, categorySlug, city, subscriptionTier, verified, rating, reviewCount, createdAt, bookingCount, paidRevenueCents, distinctCustomers]
      properties:
        id: { type: integer }
        displayName: { type: string }
        email: { type: string }
        category: { type: string }
        categorySlug: { type: string }
        city: { type: string }
        subscriptionTier: { type: string }
        verified: { type: boolean }
        rating: { type: number }
        reviewCount: { type: integer }
        createdAt: { type: string, format: date-time }
        bookingCount: { type: integer }
        paidRevenueCents: { type: integer }
        distinctCustomers: { type: integer }

    AdminCustomerRow:
      type: object
      required: [customerId, bookingCount, paidCount, totalSpentCents, firstBooking, lastBooking]
      properties:
        customerId: { type: string }
        customerName: { type: string, nullable: true }
        customerEmail: { type: string, nullable: true }
        bookingCount: { type: integer }
        paidCount: { type: integer }
        totalSpentCents: { type: integer }
        firstBooking: { type: string, format: date-time }
        lastBooking: { type: string, format: date-time }

    AdminBookingRow:
      type: object
      required: [id, customerId, providerId, providerName, serviceName, status, paymentStatus, totalPrice, scheduledAt, createdAt]
      properties:
        id: { type: integer }
        customerId: { type: string }
        customerName: { type: string, nullable: true }
        customerEmail: { type: string, nullable: true }
        providerId: { type: integer }
        providerName: { type: string }
        serviceName: { type: string }
        status: { type: string }
        paymentStatus: { type: string }
        totalPrice: { type: number }
        scheduledAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }

    AdminCategoryRow:
      type: object
      required: [slug, name, requiresDirectBilling, providerCount, bookingCount, paidRevenueCents]
      properties:
        slug: { type: string }
        name: { type: string }
        requiresDirectBilling: { type: boolean }
        providerCount: { type: integer }
        bookingCount: { type: integer }
        paidRevenueCents: { type: integer }

    HealthStatus:
      type: object
      required: [status]
      properties:
        status:
          type: string

    Category:
      type: object
      required: [id, name, slug, icon]
      properties:
        id:    { type: integer }
        name:  { type: string }
        slug:  { type: string }
        icon:  { type: string }
        description:   { type: ["string", "null"] }
        color:         { type: ["string", "null"] }
        colorLight:    { type: ["string", "null"] }
        displayOrder:  { type: integer }
        providerCount: { type: integer }
        requiresDirectBilling: { type: boolean }
        qualifications:
          type: ["object", "null"]
          additionalProperties: true

    ProviderSummary:
      type: object
      required: [id, displayName, category, city, zip, rating, reviewCount, minPrice]
      properties:
        id:          { type: integer }
        displayName: { type: string }
        category:    { type: string }
        categorySlug: { type: string }
        city:        { type: string }
        zip:         { type: string }
        rating:      { type: number }
        reviewCount: { type: integer }
        minPrice:    { type: number }
        bio:         { type: ["string", "null"] }
        avatarUrl:   { type: ["string", "null"] }
        logoUrl:     { type: ["string", "null"] }
        yearsExperience: { type: ["integer", "null"] }
        verified:    { type: boolean }
        subscriptionTier: { type: string, enum: [basic, premium] }
        requiresDirectBilling: { type: boolean }

    Provider:
      type: object
      required: [id, clerkUserId, displayName, category, city, zip, rating, reviewCount]
      properties:
        id:           { type: integer }
        clerkUserId:  { type: string }
        displayName:  { type: string }
        email:        { type: string }
        phone:        { type: ["string", "null"] }
        bio:          { type: ["string", "null"] }
        category:     { type: string }
        categorySlug: { type: string }
        city:         { type: string }
        zip:          { type: string }
        address:      { type: ["string", "null"] }
        website:      { type: ["string", "null"] }
        avatarUrl:    { type: ["string", "null"] }
        logoUrl:      { type: ["string", "null"] }
        yearsExperience: { type: ["integer", "null"] }
        rating:       { type: number }
        reviewCount:  { type: integer }
        verified:     { type: boolean }
        subscriptionTier: { type: string, enum: [basic, premium] }
        requiresDirectBilling: { type: boolean }
        premiumSince:    { type: ["string", "null"] }
        icalToken:       { type: ["string", "null"] }
        calendarSyncUrl: { type: ["string", "null"] }
        companyLegalName: { type: ["string", "null"] }
        taxId:           { type: ["string", "null"] }
        responseTime:    { type: ["string", "null"] }
        consultationMode: { type: string, enum: [online, in-person, both] }
        certificates:    { type: array, items: { type: string } }
        qualifications:
          type: ["object", "null"]
          additionalProperties: true
        createdAt:    { type: string }

    ProviderInput:
      type: object
      required: [displayName, category, city, zip]
      properties:
        displayName:  { type: string }
        bio:          { type: string }
        category:     { type: string }
        city:         { type: string }
        zip:          { type: string }
        address:      { type: string }
        phone:        { type: string }
        website:      { type: string }
        logoUrl:      { type: string }
        yearsExperience: { type: integer }
        companyLegalName: { type: string }
        taxId:        { type: string }
        responseTime: { type: string }
        consultationMode: { type: string, enum: [online, in-person, both] }
        certificates: { type: array, items: { type: string } }
        qualifications:
          type: object
          additionalProperties: true

    ProviderUpdate:
      type: object
      properties:
        displayName:  { type: string }
        bio:          { type: string }
        category:     { type: string }
        city:         { type: string }
        zip:          { type: string }
        address:      { type: string }
        phone:        { type: string }
        website:      { type: string }
        avatarUrl:    { type: string }
        logoUrl:      { type: string }
        yearsExperience: { type: integer }
        companyLegalName: { type: string }
        taxId:        { type: string }
        responseTime: { type: string }
        consultationMode: { type: string, enum: [online, in-person, both] }
        certificates: { type: array, items: { type: string } }
        qualifications:
          type: object
          additionalProperties: true

    Service:
      type: object
      required: [id, providerId, name, price, durationMinutes, vatRate]
      properties:
        id:              { type: integer }
        providerId:      { type: integer }
        name:            { type: string }
        description:     { type: ["string", "null"] }
        price:           { type: number }
        netPrice:        { type: ["number", "null"] }
        vatRate:         { type: number }
        durationMinutes: { type: integer }
        createdAt:       { type: string }

    ServiceInput:
      type: object
      required: [name, durationMinutes]
      properties:
        name:            { type: string }
        description:     { type: string }
        price:           { type: number }
        netPrice:        { type: number }
        vatRate:         { type: number }
        durationMinutes: { type: integer }

    ServiceUpdate:
      type: object
      properties:
        name:            { type: string }
        description:     { type: string }
        price:           { type: number }
        netPrice:        { type: number }
        vatRate:         { type: number }
        durationMinutes: { type: integer }

    ServiceTemplate:
      type: object
      required: [id, categorySlug, name, defaultDurationMinutes]
      properties:
        id:                     { type: integer }
        categorySlug:           { type: string }
        groupName:              { type: ["string", "null"] }
        name:                   { type: string }
        description:            { type: ["string", "null"] }
        priceType:              { type: ["string", "null"], enum: [fix, hr, pa, hoai, on, null] }
        referencePrice:         { type: ["string", "null"] }
        durationLabel:          { type: ["string", "null"] }
        defaultDurationMinutes: { type: integer }
        defaultPrice:           { type: ["number", "null"] }
        sortOrder:              { type: integer }

    UploadUrlRequest:
      type: object
      required: [name, contentType]
      properties:
        name:        { type: string }
        size:        { type: integer }
        contentType: { type: string }

    UploadUrlResponse:
      type: object
      required: [uploadURL, objectPath]
      properties:
        uploadURL:  { type: string }
        objectPath: { type: string }

    TimeSlot:
      type: object
      required: [id, providerId, startTime, endTime, isAvailable]
      properties:
        id:          { type: integer }
        providerId:  { type: integer }
        startTime:   { type: string, format: date-time }
        endTime:     { type: string, format: date-time }
        isAvailable: { type: boolean }

    TimeSlotInput:
      type: object
      required: [providerId, startTime, endTime]
      properties:
        providerId:  { type: integer }
        startTime:   { type: string, format: date-time }
        endTime:     { type: string, format: date-time }

    Booking:
      type: object
      required: [id, customerId, providerId, serviceId, slotId, status, totalPrice, scheduledAt]
      properties:
        id:           { type: integer }
        customerId:   { type: string }
        customerName: { type: ["string", "null"] }
        providerId:   { type: integer }
        providerName: { type: string }
        serviceId:    { type: integer }
        serviceName:  { type: string }
        slotId:       { type: integer }
        status:       { type: string, enum: [pending, confirmed, cancelled, completed] }
        totalPrice:   { type: number }
        scheduledAt:  { type: string, format: date-time }
        notes:        { type: ["string", "null"] }
        paymentRequired: { type: boolean }
        paymentStatus:   { type: string, enum: [not_required, pending, paid, failed, refunded] }
        stripeCheckoutSessionId: { type: ["string", "null"] }
        assessmentId:    { type: ["integer", "null"] }
        assessmentLabel: { type: ["string", "null"] }
        createdAt:    { type: string }

    BookingInput:
      type: object
      required: [providerId, serviceId, slotId]
      properties:
        providerId:    { type: integer }
        serviceId:     { type: integer }
        slotId:        { type: integer }
        notes:         { type: string }
        assessmentId:  { type: ["integer", "null"] }

    BookingStatusUpdate:
      type: object
      required: [status]
      properties:
        status: { type: string, enum: [confirmed, cancelled, completed] }

    Review:
      type: object
      required: [id, bookingId, customerId, providerId, rating, createdAt]
      properties:
        id:           { type: integer }
        bookingId:    { type: integer }
        customerId:   { type: string }
        customerName: { type: ["string", "null"] }
        providerId:   { type: integer }
        rating:       { type: integer }
        comment:      { type: ["string", "null"] }
        createdAt:    { type: string }

    ReviewInput:
      type: object
      required: [bookingId, rating]
      properties:
        bookingId:  { type: integer }
        rating:     { type: integer }
        comment:    { type: string }

    AiOfferRequest:
      type: object
      required: [providerId, inquiry]
      properties:
        providerId: { type: integer }
        inquiry:    { type: string }

    AiOfferResponse:
      type: object
      required: [offer]
      properties:
        offer:       { type: string }
        estimatedPrice: { type: ["number", "null"] }
        estimatedDuration: { type: ["string", "null"] }

    ProviderDashboard:
      type: object
      required: [totalBookings, pendingBookings, confirmedBookings, completedBookings, totalRevenue, averageRating, recentBookings]
      properties:
        totalBookings:     { type: integer }
        pendingBookings:   { type: integer }
        confirmedBookings: { type: integer }
        completedBookings: { type: integer }
        totalRevenue:      { type: number }
        averageRating:     { type: number }
        recentBookings:
          type: array
          items:
            $ref: "#/components/schemas/Booking"

    CheckoutSession:
      type: object
      required: [url]
      properties:
        url:       { type: string }
        sessionId: { type: string }

    SubscriptionStatus:
      type: object
      required: [tier]
      properties:
        tier: { type: string, enum: [basic, premium] }
        status: { type: ["string", "null"] }
        currentPeriodEnd: { type: ["string", "null"] }
        cancelAtPeriodEnd: { type: boolean }
        priceEur: { type: number }

    PlatformStats:
      type: object
      required: [totalProviders, totalBookings, totalCategories, totalReviews]
      properties:
        totalProviders:   { type: integer }
        totalBookings:    { type: integer }
        totalCategories:  { type: integer }
        totalReviews:     { type: integer }

    AssessmentInput:
      type: object
      required: [label, inputJson, resultJson]
      properties:
        label:
          type: string
          minLength: 1
          maxLength: 200
        addressJson:
          type: ["object", "null"]
          additionalProperties: true
        inputJson:
          type: object
          additionalProperties: true
        resultJson:
          type: object
          additionalProperties: true
        providerId:
          type: ["integer", "null"]

    Assessment:
      type: object
      required: [id, userId, label, inputJson, resultJson, createdAt, updatedAt]
      properties:
        id:           { type: integer }
        userId:       { type: string }
        providerId:   { type: ["integer", "null"] }
        label:        { type: string }
        addressJson:
          type: ["object", "null"]
          additionalProperties: true
        inputJson:
          type: object
          additionalProperties: true
        resultJson:
          type: object
          additionalProperties: true
        createdAt:    { type: string, format: date-time }
        updatedAt:    { type: string, format: date-time }

    GebaeudecheckPackage:
      type: object
      required: [id, credits, amountCents, label]
      properties:
        id:         { type: string }
        credits:    { type: integer }
        amountCents: { type: integer }
        label:      { type: string }

    GebaeudecheckCredits:
      type: object
      required: [balance, packages]
      properties:
        balance: { type: integer }
        packages:
          type: array
          items:
            $ref: "#/components/schemas/GebaeudecheckPackage"

    GebaeudecheckCheckoutInput:
      type: object
      required: [packageId]
      properties:
        packageId: { type: string }

    GebaeudecheckReconcileInput:
      type: object
      required: [sessionId]
      properties:
        sessionId: { type: string }

    GebaeudecheckReconcileResult:
      type: object
      required: [granted, balance]
      properties:
        granted: { type: boolean }
        balance: { type: integer }

    OfferItem:
      type: object
      required: [name, durationMinutes, netPrice, vatRate, grossPrice]
      properties:
        serviceId:       { type: ["integer", "null"] }
        name:            { type: string }
        durationMinutes: { type: integer }
        netPrice:        { type: number }
        vatRate:         { type: number }
        grossPrice:      { type: number }

    AcceptOfferInput:
      type: object
      required: [providerId, items, agbVersion]
      properties:
        providerId: { type: integer }
        inquiry:    { type: ["string", "null"] }
        offerText:  { type: ["string", "null"] }
        agbVersion: { type: string }
        items:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/OfferItem"

    OfferAcceptance:
      type: object
      required: [id, userId, providerId, items, totalNet, totalGross, agbVersion, status, createdAt]
      properties:
        id:         { type: integer }
        userId:     { type: string }
        providerId: { type: ["integer", "null"] }
        inquiry:    { type: ["string", "null"] }
        offerText:  { type: ["string", "null"] }
        items:
          type: array
          items:
            $ref: "#/components/schemas/OfferItem"
        totalNet:   { type: number }
        totalGross: { type: number }
        agbVersion: { type: string }
        status:     { type: string }
        createdAt:  { type: string, format: date-time }

    ImmobilienKundeInput:
      type: object
      required: [typ, firma]
      properties:
        typ:
          type: string
          enum: [hausverwaltung, bestandshalter]
        firma:           { type: string, minLength: 1 }
        ansprechpartner: { type: ["string", "null"] }
        telefon:         { type: ["string", "null"] }
        email:           { type: ["string", "null"] }
        anzahlGebaeude:      { type: ["integer", "null"] }
        wohneinheitenGesamt: { type: ["integer", "null"] }

    ImmobilienKunde:
      type: object
      required: [userId, typ, firma, createdAt, updatedAt]
      properties:
        userId:          { type: string }
        typ:
          type: string
          enum: [hausverwaltung, bestandshalter]
        firma:           { type: string }
        ansprechpartner: { type: ["string", "null"] }
        telefon:         { type: ["string", "null"] }
        email:           { type: ["string", "null"] }
        anzahlGebaeude:      { type: ["integer", "null"] }
        wohneinheitenGesamt: { type: ["integer", "null"] }
        createdAt:       { type: string, format: date-time }
        updatedAt:       { type: string, format: date-time }

    InvoiceSettings:
      type: object
      required: [kleinunternehmer, vatRate, nextInvoiceNumber, autoIssueInvoices]
      properties:
        kleinunternehmer:    { type: boolean }
        vatRate:             { type: string, description: "Numeric percentage as string (e.g. '19.00')" }
        invoicePrefix:       { type: ["string", "null"] }
        nextInvoiceNumber:   { type: integer }
        iban:                { type: ["string", "null"] }
        invoiceFooter:       { type: ["string", "null"] }
        autoIssueInvoices:   { type: boolean }
        companyLegalName:    { type: ["string", "null"] }
        taxId:               { type: ["string", "null"] }
        address:             { type: ["string", "null"] }
        zip:                 { type: ["string", "null"] }
        city:                { type: ["string", "null"] }

    InvoiceSettingsUpdate:
      type: object
      properties:
        kleinunternehmer:    { type: boolean }
        vatRate:             { type: string }
        invoicePrefix:       { type: ["string", "null"] }
        iban:                { type: ["string", "null"] }
        invoiceFooter:       { type: ["string", "null"] }
        autoIssueInvoices:   { type: boolean }
        companyLegalName:    { type: ["string", "null"] }
        taxId:               { type: ["string", "null"] }
        address:             { type: ["string", "null"] }

    Invoice:
      type: object
      required: [id, providerId, bookingId, invoiceNumber, kind, issuedAt, netCents, taxRate, taxCents, totalCents, currency, serviceName, status]
      properties:
        id:                  { type: integer }
        providerId:          { type: integer }
        bookingId:           { type: integer }
        invoiceNumber:       { type: string }
        kind:                { type: string, enum: [invoice, storno] }
        originalInvoiceId:   { type: ["integer", "null"] }
        issuedAt:            { type: string, format: date-time }
        netCents:            { type: integer }
        taxRate:             { type: string }
        taxCents:            { type: integer }
        totalCents:          { type: integer }
        currency:            { type: string }
        customerName:        { type: ["string", "null"] }
        customerEmail:       { type: ["string", "null"] }
        customerAddress:     { type: ["string", "null"] }
        serviceName:         { type: string }
        serviceDescription:  { type: ["string", "null"] }
        status:              { type: string }
        hasPdf:              { type: boolean }
